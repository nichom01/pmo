# Database assets

`init/001_schema.sql` initializes local PostgreSQL with core schema derived from `docs/system-design.md`.

For production-grade migration management, introduce Flyway or Liquibase in a follow-up.
